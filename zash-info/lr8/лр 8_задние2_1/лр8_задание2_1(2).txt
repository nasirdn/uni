> # ЛР08, задание 2.1(2) Атака угадывание значения функции Эйлера.
> # Закаблукова Анастасия, 3 курс, ИВТ-1.1
> 
> with(numtheory):
> with(RandomTools): 
> 
> # Алиса хочет отправить сообщение m Бобу.
> 
> q1 := nextprime(Generate(integer(range=3..10^10)));
> q2 := nextprime(Generate(integer(range=3..10^10)));
> 
> print("N = ");
> N := q1*q2;
> print("phi(N) = ");
> phN := (q1-1)*(q2-1);
> 
> e := Generate(integer(range=3..10^15)):
> while igcd(e, phi(N)) <> 1 do
>     e := Generate(integer(range=3..10^15)):
> end do:
> print("e = ", e);
> igcd(e, phi(N));
> 
> d1:= e &^ (phi(phi(N))-1) mod (phi(N));
> 
> print("m = ");
> m := 1234567890;
> 
> print("m1 = ");
> m1:= m &^ e mod N;
> 
> # ******************************************************
> 
> # Ева знает открытый ключ (e, N) и перехваченное 
> # сообщение m1, также ей известно значение функции 
> # Эйлера от числа N (phi(N)).
> 
> # Взлом разложения числа N.
> p1 := 'p1': p2 := 'p2':
>  
> sys := {(p1-p2)^2 = (p1+p2)^2-4*N, p1+p2 = N-phN+1}:
> s := solve(sys, {p1, p2}):
> assign(s);
> print("p1 = ", p1); 
> print("p2 = ", p2);
> 
> # Контроль.
> print("Контроль. phN =");
> ph_N := (p1-1)*(p2-1); 
> 
> print("Контроль. d =");
> D1 := e &^ (phi(ph_N)-1) mod ph_N;
> 
> # Расшифровка m1.
> m2 := m1 &^ D1 mod N;
> 
> print("Контроль:", m2-m);
                           5437087813
                           9022603141
                             "N = "
                      49056685579466620633
                          "phi(N) = "
                      49056685565006929680
                    "e = ", 415994831298529
                               1
                       970550900733924049
                             "m = "
                           1234567890
                            "m1 = "
                      15909698040759622427
                      "p1 = ", 5437087813
                      "p2 = ", 9022603141
                       "Контроль. phN ="
                      49056685565006929680
                        "Контроль. d ="
                       970550900733924049
                           1234567890
                         "Контроль:", 0
