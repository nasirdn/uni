> # ЛР08, задание 2.1(10) Атака с помощью выбранного шифртекста-2.
> # Закаблукова Анастасия, 3 курс, ИВТ-1.1
> 
> with(numtheory):
> with(RandomTools):
> 
> q1 := nextprime(Generate(integer(range=3..10^10)));
> q2 := nextprime(Generate(integer(range=3..10^10)));
> 
> print("n = ");
> n := q1*q2;
> 
> e := Generate(integer(range=3..10^5)):
> while igcd(e, phi(n)) <> 1 do
>     e := Generate(integer(range=3..10^5)):
> end do:
> print("e = ", e);
> igcd(e, phi(n));
> 
> d1:= e &^ (phi(phi(n))-1) mod (phi(n));
> 
> # ******************************************************
> 
> # Ева хочет получить "подпись" на текст N у Алисы.
> # Ева знает открытый ключ (e, n).
> 
> print("N = ");
> N := 12345;
> 
> N_d := N &^ d1 mod n;
> 
> x := Generate(integer(range=3..10^5));
> 
> y := x &^ e mod n;
> 
> M := y*N;
> 
> # Ева заставляет подписать число M у Алисы.
> 
> z := M &^ d1 mod n;
> 
> N1 := (x &^ (-1)) * z mod n; 
> 
> print("Контроль:", N1-N_d);

                           3733581617
                           2489812271
                             "n = "
                      9295917324786622207
                         "e = ", 29009
                               1
                      7521268412699421969
                             "N = "
                             12345
                       776918231874632976
                             84723
                      1419061452248071712
                    17518313628002445284640
                      7748699625244400088
                       776918231874632976
                         "Контроль:", 0
;
> 
;
