> # ЛР08, Задание 2.1(3). Атака с помощью выбранного шифртекста-1
> # Закаблукова Анастасия, 3 курс, ИВТ-1.1
> 
> with(numtheory):
> with(RandomTools):
> 
> # Алиса хочет отправить сообщение m Бобу
> 
> q1 := nextprime(Generate(integer(range=3..10^10)));
> q2 := nextprime(Generate(integer(range=3..10^10)));
> 
> print("N = ");
> N := q1*q2;
> 
> e := Generate(integer(range=3..10^10)):
> while igcd(e, phi(N)) <> 1 do
>     e := Generate(integer(range=3..10^10)):
> end do:
> print("e = ", e);
> igcd(e, phi(N));
> 
> d1:= e &^ (phi(phi(N))-1) mod (phi(N));
> 
> print("m = ");
> m := 123456789;
> 
> print("c = ");
> c:= m &^ e mod N;
> 
> # ******************************************************
> 
> # Ева знает открытый ключ (e, N) и перехваченное
> # сообщение c.
> 
> r := Generate(integer(range=3..10^5)):
> if r > N then
>    r := Generate(integer(range=3..10^5));
> end if:
> print("r = ", r);
> 
> x := r &^ e mod N;
> y := x*c mod N;
> t := r &^ (-1) mod N;
> 
> # Ева обманным путем заставляет Боба "подписать" число y.
> u := y &^ d1 mod N;
> 
> m2 := t*u mod N;
> 
> print("Контроль:", m2-m);

                           8497348807
                           7015461389
                             "N = "
                      59612822464373712923
                       "e = ", 3577586555
                               1
                      2513869939565171555
                             "m = "
                           123456789
                             "c = "
                      50628353027442258452
                         "r = ", 49761
                      39900906442433614024
                      14637347934455236989
                      9226863580326085427
                         6143333277429
                           123456789
                         "Контроль:", 0
;
> 
;
