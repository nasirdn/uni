> # ЛР08, Задание 2.1(3). Атака с помощью разложения
> # на множители методом Ферма.
> # Закаблукова Анастасия, 3 курс, ИВТ-1.1
> 
> with(numtheory):
> with(RandomTools):
> 
> # Алиса хочет отправить сообщение m Бобу.
> 
> q1 := nextprime(Generate(integer(range=3..10^5)));
> q2 := nextprime(Generate(integer(range=3..10^5)));
> 
> print("N = ");
> n := q1*q2;
> 
> e := Generate(integer(range=3..10^15)):
> while igcd(e, phi(n)) <> 1 do
>      e := Generate(integer(range=3..10^15)):
> end do:
> print("e = ", e);
> igcd(e, phi(n));
> 
> d1:= e &^ (phi(phi(n))-1) mod (phi(n));
> 
> print("m = ");
> m := 1234567890;
> 
> print("m1 = ");
> m1:= m &^ e mod n;
> 
> # ******************************************************
> 
> # Ева знает открытый ключ (e, N) и перехваченное
> # сообщение m1.
> 
> k := 1 + floor(sqrt(n));
> y := k^2 - n;
> b := 1;
> 
> 
> while (floor(sqrt(y)) <> sqrt(y)) do
>     y := y + 2*k + b:
>     b := b + 2:
> 
>     if (floor(sqrt(y)) >= n/2)then
>         print("Множители не найдены"):
>     break
>     end if:
> end do:
> 
> x := sqrt(n + y);
> y := sqrt(y);
> 
> print("p1 = ");
> p1 := x-y;
> 
> print("p2 = ");
> p2 := x+y;
> 
> # Контроль.
> print("Контроль. N ="); 
> N :=  p1*p2; 
> phN := (p1-1)*(p2-1);
> 
> # Вычисление закрытого ключа D1.
> D1:= e &^ (phi(phN)-1) mod phN;
> print("Контроль. d =", D1);
> 
> # Расшифровка m1
> m2 := m1 &^ D1 mod N;
> 
> print("Контроль:", m2-m);
> 
> 
> 
                             61283
                             56081
                             "N = "
                           3436811923
                    "e = ", 251419267425877
                               1
                           3237134493
                             "m = "
                           1234567890
                            "m1 = "
                           2991035064
                             58625
                             78702
                               1
                             58682
                              2601
                            "p1 = "
                             56081
                            "p2 = "
                             61283
                        "Контроль. N ="
                           3436811923
                           3436694560
                           3237134493
                  "Контроль. d =", 3237134493
                           1234567890
                         "Контроль:", 0

