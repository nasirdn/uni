> #  ЛР08, задание 2.1(4) Атака с использованием общего модуля
> # Закаблукова Анастасия, 3 курс, ИВТ-1.1
> 
> with(numtheory):
> with(RandomTools):
> 
> # Алиса хочет отправить сообщение m Бобу, а также Боб
> # хочет отправить Алисе то же сообщение m.
> 
> q1 := nextprime(Generate(integer(range=3..10^10)));
> q2 := nextprime(Generate(integer(range=3..10^10)));
> 
> # Общий модуль Na = Nb = N.
> print("N = ");
> N := q1*q2;
> print("phi(N) = ");
> phN := (q1-1)*(q2-1);
> 
> # Ключи Алисы.
> e_a := Generate(integer(range=3..10^15)):
> while igcd(e_a, phN) <> 1 do
>     e_a := Generate(integer(range=3..10^15)):
> end do:
> 
> print("e_a = ", e_a);
> igcd(e_a, phN);
> 
> d1_a:= e_a &^ (phi(phN)-1) mod phN;
> 
> # Ключи Боба.
> e_b := Generate(integer(range=3..10^15)):
> while igcd(e_b, phN) <> 1 do
>    e_b := Generate(integer(range=3..10^15)):
> end do:
> 
> print("e_b = ", e_b);
> igcd(e_b, phN);
> 
> d1_b:= e_b &^ (phi(phN)-1) mod phN;
> 
> igcd(e_a, e_b);
> 
> print("m = ");
> m := 237135;
> 
> print("Алиса посылает. m1 = ");
> c_a:= m &^ e_b mod N;
> 
> print("Боб посылает. m1 = ");
> c_b:= m &^ e_a mod N;
> 
> # ******************************************************
> 
> # Ева знает модуль N и открытые ключи e_a и e_b, а также
> # перехваченые сообщения c_a и c_b.

> igcdex(e_b, e_a, 'x', 'y');
> print("x =", x);
> print("y =", y);
> 
> d_a := c_a &^ abs(x) mod N;
> d_b := c_b &^ abs(y) mod N;
> 
> print("Вычисляем d_a^(-1) или d_b^(-1).");
> if x < 0 then
>     d_a := d_a &^ (-1) mod N;
> end if;
> if y < 0 then
>     d_b := d_b &^ (-1) mod N;
> end if;
> 
> m2 := d_a * d_b mod N;
> 
> print("Контроль:", m2-m);
                           6253613413
                           3323948773
                             "N = "
                      20786690630957692249
                          "phi(N) = "
                      20786690621380130064
                   "e_a = ", 500971744630267
                               1
                      14775524047797132595
                   "e_b = ", 990474763709579
                               1
                      10119818401094937107
                               1
                             "m = "
                             237135
                    "Алиса посылает. m1 = "
                      18311090409694118409
                     "Боб посылает. m1 = "
                       876061631746101429
                               1
                     "x =", 32756677501033
                     "y =", -64763457730918
                      9834715757120071340
                      10032018234236659327
               "Вычисляем d_a^(-1) или d_b^(-1)."
                      1077317756080790682
                             237135
                         "Контроль:", 0

