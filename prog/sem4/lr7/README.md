1. "+" Переписать функцию по добавлению данных (CREATE) в таблицу с помощью параметризованных запросов (named-style)
2. "+" Реализовать сохранение реального текущего времени и других данных
3. "+" Создать класс для счетчика с методами increment, decrement, reset
4. "+" Протестировать подключение к БД не в памяти (не использовать ``:memory:``)